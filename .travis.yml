dist: bionic
sudo: false
language: python

addons:
  apt:
    packages:
      - liblzo2-dev
      - libsnappy-dev

python:
  - 2.7
  - 3.5
  - 3.6
  - 3.7

matrix:
  include:
    - python: 3.7
      env: TOXENV=alternate-zstd

install:
  - pip install tox tox-travis

script: tox

notifications:
  email: false
